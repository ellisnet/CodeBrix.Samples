<Page
    x:Class="Pinta.Brix.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:Pinta.Brix.ViewModels"
    FontFamily="{StaticResource OpenSansFont}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:MainViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Menu -->
        <MenuBar Grid.Row="0">
            <MenuBarItem Title="File">
                <MenuFlyoutItem x:Name="MenuFileNew" Text="New" Click="MenuFileNew_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="N" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="MenuFileOpen" Text="Open…" Click="MenuFileOpen_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="O" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutSeparator />
                <MenuFlyoutItem x:Name="MenuFileSave" Text="Save" Click="MenuFileSave_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="S" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="MenuFileSaveAs" Text="Save As…" Click="MenuFileSaveAs_Click" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem x:Name="MenuFileClose" Text="Close" Click="MenuFileClose_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="W" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
            <MenuBarItem Title="Edit">
                <MenuFlyoutItem x:Name="MenuEditUndo" Text="Undo" Click="MenuEditUndo_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control" Key="Z" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
                <MenuFlyoutItem x:Name="MenuEditRedo" Text="Redo" Click="MenuEditRedo_Click">
                    <MenuFlyoutItem.KeyboardAccelerators>
                        <KeyboardAccelerator Modifiers="Control,Shift" Key="Z" />
                    </MenuFlyoutItem.KeyboardAccelerators>
                </MenuFlyoutItem>
            </MenuBarItem>
            <MenuBarItem Title="View">
                <MenuFlyoutItem x:Name="MenuViewZoomIn" Text="Zoom In" Click="MenuViewZoomIn_Click" />
                <MenuFlyoutItem x:Name="MenuViewZoomOut" Text="Zoom Out" Click="MenuViewZoomOut_Click" />
                <MenuFlyoutItem x:Name="MenuViewZoomNormal" Text="Normal Size" Click="MenuViewZoomNormal_Click" />
                <MenuFlyoutItem x:Name="MenuViewZoomFit" Text="Best Fit" Click="MenuViewZoomFit_Click" />
            </MenuBarItem>
            <MenuBarItem Title="Adjustments" x:Name="AdjustmentsMenu" />
            <MenuBarItem Title="Effects" x:Name="EffectsMenu" />
        </MenuBar>

        <!-- Tool options toolbar -->
        <Border Grid.Row="1" BorderThickness="0,1,0,1" BorderBrush="{ThemeResource SystemControlForegroundBaseLowBrush}">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <StackPanel x:Name="ToolOptionsPanel" Orientation="Horizontal" MinHeight="40" Padding="6,2" Spacing="2" />
            </ScrollViewer>
        </Border>

        <!-- Main content: toolbox | canvas tabs | pads -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ToolboxPanel" Width="72" Padding="4" />
            </ScrollViewer>

            <TabView x:Name="DocumentTabs"
                     Grid.Column="1"
                     IsAddTabButtonVisible="False"
                     TabCloseRequested="DocumentTabs_TabCloseRequested"
                     SelectionChanged="DocumentTabs_SelectionChanged" />

            <Grid Grid.Column="2" Width="230">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Layers" FontWeight="SemiBold" Margin="8,6,8,2" />
                    <ListView x:Name="LayersList" Grid.Row="1" SelectionChanged="LayersList_SelectionChanged" />
                    <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" Margin="8,2,8,6">
                        <Button x:Name="AddLayerButton" Content="+" Click="AddLayerButton_Click" />
                        <Button x:Name="RemoveLayerButton" Content="−" Click="RemoveLayerButton_Click" />
                    </StackPanel>
                </Grid>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="History" FontWeight="SemiBold" Margin="8,6,8,2" />
                    <ListView x:Name="HistoryList" Grid.Row="1" />
                </Grid>
            </Grid>
        </Grid>

        <!-- Status bar -->
        <Grid Grid.Row="3" Padding="8,4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusText" Grid.Column="0" VerticalAlignment="Center" />
            <TextBlock x:Name="CursorPositionText" Grid.Column="1" VerticalAlignment="Center" Margin="12,0" />
            <ComboBox x:Name="ZoomComboBox" Grid.Column="2" MinWidth="90" SelectionChanged="ZoomComboBox_SelectionChanged" />
        </Grid>
    </Grid>
</Page>
