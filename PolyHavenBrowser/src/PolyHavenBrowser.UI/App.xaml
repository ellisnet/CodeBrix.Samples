<Application x:Class="PolyHavenBrowser.App"
       xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
       xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
       RequestedTheme="Dark">

  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <!-- Load WinUI resources -->
        <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
      </ResourceDictionary.MergedDictionaries>
      <!-- Roboto font - reference the .ttf file directly (the Fonts.xaml
           merge does not work on Skia targets) -->
      <FontFamily x:Key="RobotoFont">ms-appx:///CodeBrix.Platform.Fonts.Roboto/Fonts/Roboto.ttf</FontFamily>

      <!-- Dialogs open in the popup layer, which follows the app default theme (the
           RequestedTheme="Dark" above) rather than RootGrid's - these ContentDialog
           keys then refine them to the app palette. On the FrameBuffer heads the
           built-in picker/software-keyboard chrome resolves the same keys, so it
           restyles identically -->
      <SolidColorBrush x:Key="ContentDialogBackground" Color="#1F232B" />
      <SolidColorBrush x:Key="ContentDialogForeground" Color="#F2F4F8" />
      <SolidColorBrush x:Key="ContentDialogBorderBrush" Color="#2A2F39" />
      <SolidColorBrush x:Key="ContentDialogLightDismissOverlayBackground" Color="#99000000" />
      <!-- Resolved by the FrameBuffer/Emulated picker + software-keyboard chrome -->
      <SolidColorBrush x:Key="ContentDialogTopOverlay" Color="#1F232B" />
      <SolidColorBrush x:Key="ContentDialogSeparatorBorderBrush" Color="#2A2F39" />
      <SolidColorBrush x:Key="ContentDialogSmokeFill" Color="#4D000000" />
    </ResourceDictionary>
  </Application.Resources>

</Application>
