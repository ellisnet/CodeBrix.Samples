<Window x:Class="WikipediaPublisher.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WikipediaPublisher.ViewModels"
        xmlns:local="clr-namespace:WikipediaPublisher.Views"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Wikipedia Publisher" Background="#F4F4F4"
        Height="800" Width="1100" MinHeight="600" MinWidth="800">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Search bar -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Search Wikipedia:"
                   VerticalContentAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="1" Height="30" VerticalContentAlignment="Center"
                     Text="{Binding SearchTerms, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <!-- Pressing Enter in the search box runs Search, just like clicking the button. -->
                <TextBox.InputBindings>
                    <KeyBinding Key="Return" Command="{Binding SearchCommand}" />
                </TextBox.InputBindings>
            </TextBox>
            <Button Grid.Column="2" Width="100" Height="30" Margin="10,0,0,0"
                    Content="Search"
                    Command="{Binding SearchCommand}" />
        </Grid>

        <!-- Embedded browser -->
        <wv2:WebView2 Grid.Row="1" x:Name="Browser"
                      Source="https://en.wikipedia.org/wiki/Main_Page" />

        <!-- Status line -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ProgressBar Grid.Column="0" Height="8" VerticalAlignment="Center"
                         Minimum="0" Maximum="100"
                         Value="{Binding ProgressValue}" />
            <TextBlock Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding StatusText}" />
        </Grid>

        <!-- Bottom bar: output file path (+ Select dialog), page size, Publish -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0" Content="Save PDF to:"
                   VerticalContentAlignment="Center" Margin="0,0,10,0" />
            <TextBox Grid.Column="1" Height="30" VerticalContentAlignment="Center"
                     Text="{Binding OutputFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="2" Height="30" Margin="8,0,0,0" Padding="12,0"
                    Content="Select…"
                    Command="{Binding SelectOutputFileCommand}" />
            <Label Grid.Column="3" Content="Page size:"
                   VerticalContentAlignment="Center" Margin="14,0,10,0" />
            <ComboBox Grid.Column="4" Width="200" Height="30" VerticalContentAlignment="Center"
                      ItemsSource="{Binding PageSizeNames}"
                      SelectedItem="{Binding SelectedPageSizeName, Mode=TwoWay}" />
            <Button Grid.Column="5" Width="120" Height="34" Margin="14,0,0,0"
                    Content="Publish" FontWeight="SemiBold"
                    Command="{Binding PublishCommand}" />
        </Grid>
    </Grid>
</Window>
